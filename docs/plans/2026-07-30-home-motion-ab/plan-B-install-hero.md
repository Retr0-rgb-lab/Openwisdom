# Plan B — Install + Hero

## Deliver

### InstallCommand.tsx

- Code row: one-shot **sweep** highlight on first view or focus (CSS animation, RM off)
- Copy button: scale press + ClickSpark (existing) stronger
- Optional wrap copy with **Magnet** (desktop)
- Optional display command via **TextType** once when `emphasis` (Hero/Final); RM full string
- Keep CLI-only honesty

### Hero.tsx

- Keep centered, no right diagram
- Add **ShapeGrid** or small 5×5 panel under triad (once settle, transform only, C7 safe)
- Triad glyphs: once settle stagger
- Install enter kept; wire TextType + Magnet if bits exist
- Focal sequence: title → sub → triad → install

## Sources label

- ShapeGrid: react-bits Shape Grid adapted  
- TextType: react-bits TextType adapted  
- Magnet: react-bits Magnet adapted  
- Sweep: ow-custom CSS  

## Out of scope

ScenarioCards, Model, SiteBackdrop internals (only import bits).
